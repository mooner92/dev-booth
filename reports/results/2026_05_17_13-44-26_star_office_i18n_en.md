# Star-Office-UI — English default + Han-character cleanup

**Date:** 2026-05-17
**Repo:** `~/star-office-ui` (user-local install — not under dev-booth git)
**Service:** `systemctl --user star-office-ui.service`
**Result:** ✅ default lang EN, listed Chinese strings → English, service active

---

## 1. Changes

### `frontend/index.html`
| Where | Before | After |
|-------|--------|-------|
| line 2 | `<html lang="zh-CN">` | `<html lang="en">` |
| line 1144 | `正在进入像素办公室…` | `Loading pixel office…` |
| line 1145 | `加载中...` (game status) | `Loading...` |
| line 1152 | `<div id="memo-title">昨 日 小 记</div>` | `Yesterday Notes` |
| line 1156 | `<div id="memo-placeholder">加载中...</div>` | `Loading...` |
| line 1163 | `Star 状态` | `Agent Status` |
| lines 1165-1169 | `待命/工作/同步/报警/装修房间` | `Idle/Work/Sync/Alert/Decorate Room` (button labels + setState args) |
| line 1175 | `访 客 列 表` | `Team Agents` |
| line 1177 | `正在加载访客...` | `Loading agents...` |
| line 1320 (EN locale) | `memoTitle: '어제 작업 노트', guestTitle: '팀 에이전트'` | `'Yesterday Notes', 'Team Agents'` |
| line 1562 | `어제 작업 내역이 없습니다` | `No notes from yesterday` |
| line 3427 | `오프라인` (no-visitor placeholder) | `No agents online` |
| lines 3432-3454 (visitor render) | `未命名访客 / 已授权 / 待授权 / 已拒绝 / 离线 / 待命 / 工作 / 调研 / 执行 / 同步 / 报警 / 交换 skill 功能开发中 / 交换skill / 离开房间` | `Unnamed agent / Approved / Pending / Rejected / Offline / Idle / Working / Researching / Current Task / Syncing / Alert / Switch Skill — Coming Soon / Switch Skill / Leave` |
| visitor subtitle | `${statusText} · ${stateText}` | `${statusText} · Status: ${stateText}` |

The visitor subtitle now reads `Approved · Status: Idle` — surfaces the "Status" label the user specified for the "传令" position.

### `frontend/game.js` (lines 102-109, `STATES` dict)
| key | Before | After |
|-----|--------|-------|
| `idle` | `待命` | `Idle` |
| `writing` | `整理文档` | `Working` |
| `researching` | `搜索信息` | `Researching` |
| `executing` | `执行任务` | `Current Task` |
| `syncing` | `同步备份` | `Syncing` |
| `error` | `出错了` | `Error` |

### Default-language behavior
`let uiLang = localStorage.getItem('uiLang') || 'en';` (line 1293) was already EN-default — no change needed. CN/JP locale dictionaries left intact so the EN/JP/CN toggle still works.

---

## 2. Live verification

```
$ systemctl --user is-active star-office-ui
active

$ curl -s http://localhost:19000/ | grep -c 'lang="en"'
1

$ curl -s http://localhost:19000/ | grep -c 'Loading agents'
1

$ curl -s http://localhost:19000/ | grep -c 'Team Agents'
2   ← static <div> + EN-locale i18n entry

$ curl -s http://localhost:19000/ | grep -c 'Yesterday Notes'
2

$ curl -s http://localhost:19000/ | grep -c 'Agent Status'
1
```

---

## 3. Out-of-scope (intentional)

- Asset drawer panel labels (lines ~1194-1267) and the asset description dictionary (lines ~1900+) still hold Chinese strings — those are only visible after clicking 🪚 / 🤝, and the user's spec listed only the surface-level visible chrome.
- `BUBBLE_TEXTS` flavor lines in `game.js` (~lines 113-180) — these are atmospheric cat bubbles that only appear briefly; out of the listed scope.
- CN locale dictionary entries (lines 1300-1320 ish) preserved so CN button still works.

---

## 4. Operator follow-up

```bash
# No sudo needed — service is user-level
systemctl --user restart star-office-ui   # already ran during this Ralph
```

The user-level service `star-office-ui.service` was restarted; serves new static HTML immediately on next request.

---

## 5. 금지 사항 준수

- ✅ 픽셀 아트 에셋 미수정
- ✅ i18n 구조 (setUILanguage 함수) 유지 — EN/JP/CN 토글 정상
- ✅ 보고서 파일명 `2026_05_17_13-44-26_star_office_i18n_en.md` (콜론 없음)
