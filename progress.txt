# Dev-Booth Dashboard Redesign v8 — Ralph progress

## Plan (in-prompt)
Layout: 3-col (FileTree | Chat | Kanban+GPU) → 2-col (Kanban sidebar 240px | Chat+ProgressBar).
Kanban: single vertical list ordered by created_at, status icons (✓/●animated/⊘/▶/⬜), TaskRow click→logs.
GPU: small summary at sidebar bottom (util/mem/temp from /api/metrics/preset).
Chat: top progress bar (done/total) + currentStageName; ChatStream tabs unchanged.
ChatMessage: processBody (unicode unescape + tool_response/output tag strip); tool-call rows get bg-muted/40.

## Inheritance
Builds on v7 skill activation (commits c72c27b → 91ed31e, all local).

## Phase 0 (DONE)
- GPU data path confirmed: api.getMetricsPreset(preset) for gpu_utilization/gpu_memory_used/gpu_temperature (same as MonitoringPane uses).
- No `sidebar` CSS token — use `bg-card` / `bg-background` / `bg-muted`.
- AppHeader exists but optional; new in-page top bar replaces it.
- pytest baseline 153 (v7 carry-over). Frontend has no unit tests; tsc + npm run build = the gate.

## Workers (in flight)
- worker-A (designer/sonnet): KanbanBoard.tsx full rewrite + SessionDetailClient.tsx 3→2 col + progress bar + drop FileTreePane usage.
- worker-B (executor/sonnet): ChatMessage.tsx processBody helper + tool-call bg.

## Operator TODOs (post-merge)
1. sudo systemctl restart dev-booth-dashboard
2. DEV_BOOTH_DRYRUN=0 git push origin feat/kanban-redesign-2026-05-14
